/** default max traversal concurrency */
export const DEFAULT_CONCURRENCY = 25;

/** max page size per Notion API request */
export const MAX_PAGE_SIZE = 100;

/** default max requests per proactive rate-limit interval */
export const DEFAULT_RATE_LIMIT_COUNT = 3;

/** default proactive rate-limit interval window in milliseconds */
export const DEFAULT_RATE_LIMIT_INTERVAL = 1000;
